# AID Protocol Constants - Single Source of Truth
# This file defines all constants, types, and tokens used in the AID specification
# Generated code in all languages should derive from this file

schemaVersion: '2.0.0'

# Default specification version generated by tooling.
specVersion: 'aid2'

# Major versions supported by compatibility clients.
supportedSpecVersions:
  - 'aid1'
  - 'aid2'

# Protocol tokens (proto/p field)
protocolTokens:
  mcp: 'Model Context Protocol'
  a2a: 'Agent-to-Agent Protocol'
  openapi: 'URI points to an OpenAPI specification document'
  grpc: 'gRPC over HTTP/2 or HTTP/3'
  graphql: 'GraphQL over HTTP'
  websocket: 'WebSocket transport'
  local: 'The agent runs locally on the client machine'
  zeroconf: 'mDNS/DNS-SD service discovery'
  ucp: 'Universal Commerce Protocol'

# Authentication scheme tokens (auth field)
authTokens:
  none: 'No authentication required'
  pat: 'Personal Access Token'
  apikey: 'API Key authentication'
  basic: 'HTTP Basic Authentication'
  oauth2_device: 'OAuth 2.0 Device Flow'
  oauth2_code: 'OAuth 2.0 Authorization Code Flow'
  mtls: 'Mutual TLS'
  custom: 'Custom authentication scheme'

# Standard client error codes
errorCodes:
  ERR_NO_RECORD:
    code: 1000
    description: 'No _agent TXT record was found for the domain'
  ERR_INVALID_TXT:
    code: 1001
    description: 'A record was found but is malformed or missing required keys'
  ERR_UNSUPPORTED_PROTO:
    code: 1002
    description: 'The record is valid, but the client does not support the specified protocol'
  ERR_SECURITY:
    code: 1003
    description: 'Discovery failed due to a security policy (e.g., DNSSEC failure, local execution denied)'
  ERR_DNS_LOOKUP_FAILED:
    code: 1004
    description: 'The DNS query failed for a network-related reason'
  ERR_FALLBACK_FAILED:
    code: 1005
    description: 'The .well-known fallback failed or returned invalid data'

# AID v2 record structure - required and optional fields.
# Compatibility clients still parse aid1 records with legacy PKA kid/i rules.
aidRecord:
  required:
    - v # Version (must be "aid2" for the current spec)
    - uri # Absolute https:// URL or package URI
    - proto # Protocol token (or alias 'p')
  optional:
    - auth # Authentication hint token
    - desc # Human-readable description (≤ 60 UTF-8 bytes)
    - docs # Absolute https:// URL to human docs
    - dep # ISO 8601 UTC deprecation timestamp
    - pka # Unpadded base64url Ed25519 JWK x (32 bytes)

  aliases:
    p: proto # Shorthand alias for proto field
    u: uri # Alias for uri
    a: auth # Alias for auth
    s: desc # Alias for desc
    d: docs # Alias for docs
    e: dep # Alias for dep
    k: pka # Alias for pka

# URI schemes allowed for local protocol
localUriSchemes:
  - docker
  - npx
  - pip

# DNS configuration recommendations
dns:
  subdomain: '_agent'
  ttlRecommendation:
    min: 300 # 5 minutes
    max: 900 # 15 minutes

# Optional HTTPS fallback for DNS-restricted environments
wellKnown:
  path: '/.well-known/agent'
  errorCode: 'ERR_FALLBACK_FAILED'
